# 这是一个注释（以 # 开头的内容会被 Git 忽略）

# 1. 忽略指定名称的单个文件
config.json

# 2. 忽略特定后缀的所有文件（使用通配符 *）
*.mat
*.segd

# 3. 忽略整个文件夹（末尾加斜杠 /）
__pycache__/
output/
.ipynb_checkpoints/

# 4. 例外规则（用感叹号 ! 表示：虽然忽略了所有 .log，但保留 important.log）
!important.log